[package]
name = "escrow-factory"
version = "0.0.1"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
milestone-escrow = { path = "../../escrow/milestone-escrow" }
farmland-token = { path = "../../templates/farmland-token" }
